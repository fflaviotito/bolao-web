import styled from 'styled-components';

export const CampoCompartilhado = styled.div`
    display: flex;
    align-items: center;
    gap: 12px;

    > img {
        width: 30px;
        object-fit: contain;
    }
`;
